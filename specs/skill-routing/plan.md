# Skill routing — a door for the request that does not name one
Specs-Contract: process-first-ready-v1

## Scope decision (GATE-SCOPE — 2026-09-21)
- Chosen: **KEEP**. Rewrite the `description` field of `cf:specs` and `cf:brainstorm` in the source tree, measure the existing two-door case again, and record the result. Nothing else.
- Target chosen by the user: **fewer runs that invoke no skill at all**. Which of the two doors a run takes is not the bar; a run that reaches either door is a win, and a run that answers directly is the loss this packet aims at.
- Doors: **two**, `cf:specs` and `cf:brainstorm`, so every count stays directly comparable with the 2026-09-21 baseline. Six or thirty-two doors were offered and declined.
- Editable: **both `description` fields**. The catalog fields `when_to_use`, `keywords` and `category` were offered as a third option and not chosen, so they stay fixed.
- Expansion signals: two modified source files, no new service or class, one independently deliverable group — all below the thresholds, which is why KEEP is the recorded decision rather than a compromise.

## Completion decision (GATE-DONE — 2026-09-21)
The user accepted the feature as complete with all three tasks `done`, each carrying a canonical inline Receipt, and all six acceptance criteria met. The measurement is recorded here split by model, because the pooled figure alone would misdescribe it.

| Reading | Baseline | This measurement | Two-sided Fisher |
|---|---|---|---|
| Runs invoking no door, pooled | 9/20 | 4/40 | p = 0.0057, inside the 7/40 resolution recorded before the run |
| sonnet | 6/10 | 0/20 | p = 0.0004 |
| opus | 3/10 | 4/20 | p = 0.66, not separable from noise |
| Negative cases, skill invoked | 0/24 | 0/12 | held; excludes an over-trigger rate above roughly 22% and no lower |

What was bought: **sonnet no longer leaves the request unrouted, and opus is unchanged as far as twenty runs against a ten-run baseline can tell.** The packet was motivated by opus's shape, so the honest summary is that the description fixed one model and left the other's failure untouched.

The user's decision was to accept completion and to treat opus as a separate packet, because the four remaining unrouted runs share one cause that is not a description problem: each reports that Write, Edit and Bash are unavailable and then delivers the implementation as code to paste. A description cannot reach that behaviour, so the next intervention is a different kind and deserves its own scope decision.

Limitations carried out of this packet: opus is unrepaired and unreadable at this sample size; the remainder's composition worsened, from three harmful and six harmless unrouted runs to four harmful and none harmless, because the harmless shape was sonnet's; run 17 is a disputed `khong-tu-chot` failure where three judge votes and the rubric's own text disagree, which decides 15/20 against 16/20; `cf:brainstorm` at 0/40 tests nothing, so narrowing it remains unmeasured in either direction; that narrowing leaves three body surfaces without a catalogue entry; the `materialChoice` clause now sits 42 characters from a full match and will halt the suite rather than fail a test if a later rewrite completes it; opus's turn distribution moved and cannot be chased because traces are deleted; the installed `.claude/` copy stays two generations behind, so this repository's own sessions do not run what was measured; and the whole measurement lives in a two-door world while the product ships thirty-two skills.


## Why this is wanted
`specs-a-plus` closed on 2026-09-21 with three of five AC-07 bars met and both misses traced to one cause: the skill is followed whenever it is reached, and the failures sit in runs that reach nothing. Its task 10 measured the ambiguous request ten times per model with both doors loaded: `cf:specs` 11/20, `cf:brainstorm` 0/20, **no skill at all 9/20** (sonnet 6/10, opus 3/10). The retained final messages show what the unrouted runs did — all three unrouted opus runs settled the undecided questions themselves and delivered finished code, while every routed opus run passed every grader. The lever was deliberately left untouched there so the measurement could not defend itself; this packet moves it.

## Measured constraints (2026-09-21, HEAD `6f32472`, worktree clean)
- Each description has exactly **one git-tracked home**: `packages/spec/src/claude/skills/specs/SKILL.md:3` and `packages/spec/src/claude/skills/brainstorm/SKILL.md:3`. A recursive search of `packages/spec/src`, `packages/spec/scripts` and `docs` for the current `cf:specs` description returns that one file. `packages/spec/src/` also holds `codex/`, `common/` and `omp/`, and none of them contains a `specs` skill file.
- `.claude/` and `packages/spec/.agents/` are gitignored (`.gitignore:115-116`) and hold **0 git-tracked files**; there is no `.agents/` at the repository root. Only `.claude/skills/specs/SKILL.md:3` is stale, still carrying the pre-2026-09-17 description that measured 0/4, while `.claude/skills/brainstorm/SKILL.md` is byte-identical to source. They are install artifacts regenerated by the installer, not edit targets. The eval harness copies the skill from the source tree (`evals/run.sh:28`), so the measurement reads the edited file and not these.
- For `cf:specs` the suite constrains the frontmatter but not the wording: `description:` must be a non-empty single line and `argument-hint: "<feature-description>"` is pinned exactly (`run-skill-self-tests.mjs:2719-2720`, `:4258-4263`). Two whole-file checks still reach the description line: `:4264` rejects `--status`, `--validate` or `--archive` anywhere in the file, and `:5525` rejects the word `lane` outside the legacy sections. No probe contains any part of the current text.
- **For `cf:brainstorm` the description is inside a semantic corpus and the wording is constrained.** `brainstormSemanticMarkdown` (`run-skill-self-tests.mjs:1392-1398`) strips comments, code fences and strikethrough but **not the YAML frontmatter**, so the description line is part of `normalized.skill`, which `:1719-1720` scans with `brainstormHasWorkflowDispatch(..., "specs", true)` and `(..., "develop")`; a hit adds the `handoff-boundary` issue and `:1843-1844` fails the whole suite. A coordination verb (`invoke`, `start`, `dispatch`, `launch`, `route`, `forward`, `run`, `execute`, `hand off`) near `specs` or `develop` therefore breaks 1366 tests. Naming the other door without such a verb is safe; the reviewer confirmed this by running the extracted functions over four candidate lines, of which three failed.
- `SKILL.md` has a 150–230 line floor and ceiling and the nine-file bundle sits at **745/750**, counted file by file today (150 + 129 + 212 + 82 + 8 + 42 + 19 + 56 + 47); a one-line description that stays one line changes neither. The suite prints no total unless it exceeds the ceiling (`run-skill-self-tests.mjs:1016`), so the figure comes from counting, not from the run.
- Baseline before any edit, measured today: `node scripts/run-skill-self-tests.mjs` from `packages/spec` exits 0 and prints `[skill-test] PASS: 1366 tests executed`. It takes about ten minutes, longer than every other command in this packet, so task 01 should expect to wait rather than assume it hung.
- **Resolution of the measurement, computed before spending anything.** Against the baseline of 9/20, a two-sided Fisher exact test on twenty new runs separates only 0/20, 1/20 and 2/20 from noise (p = 0.001, 0.008, 0.031); 3/20 through 7/20 cannot be told apart from the baseline (p = 0.082 to 0.748). Per model at ten runs nothing is readable at all: opus falling from 3/10 to 0/10 gives p = 0.211. With forty new runs — twenty per model, the figure GATE-REVIEW chose — the readable ceiling rises to 7/40, that is 18% against the baseline's 45% (p = 0.032), so a genuine halving becomes visible instead of silent. These figures were computed in this repository and the per-model one corrects the reviewer's report, which stated p = 0.028 for opus at 0/10.
- The instrument exists and is proven: `evals/run.sh --with-skill` is repeatable (`evals/run.sh:36-43`), and `evals/specs/mo-ho-du-cua/` carries a prompt byte-identical to `mo-ho-c1`, a door-neutral rubric, and three collision-resistant door graders.

## Out of scope
- The body of either `SKILL.md`; only the frontmatter `description` line changes.
- `when_to_use`, `keywords` and `category`, and `rules/skill-domain-routing.md`.
- The descriptions of the other thirty skills in `packages/spec/src/claude/skills/`.
- Re-syncing the stale `.claude/` and `.agents/` installs.
- Any change to `evals/run.sh`, the case, the graders, or the summarizer.
- Measuring with more than two doors, and any per-model floor.

## Coverage profile
| ID | Outcome | Change kinds | Material surfaces | Ambiguity/action | Risk/evidence | Required proof |
|---|---|---|---|---|---|---|
| CP-01 | Both descriptions cover the request that does not say enough to act on, and divide the two doors by a condition a reader can check | modify | `skills/specs/SKILL.md:3`, `skills/brainstorm/SKILL.md:3` | none — the target and the editable fields were settled at GATE-SCOPE | elevated — the description is the text every host shows a model, so this changes routing for every user request; evidence: no probe pins the wording (`run-skill-self-tests.mjs:2719`, `:4262`) | source: self-test PASS with printed totals, plus the two lines quoted in the Receipt |
| CP-02 | The suite that measures routing still loads unchanged after the rewrite | verify | `evals/specs/*`, `evals/run.sh` | none | routine — no file in the suite is edited; evidence: `--validate` exits 0 only when every case loads (`evals/run.sh:65-78`) | source: `--validate` output with no `✗` |
| CP-03 | The two-door counts are measured again and placed beside the baseline, per model | verify | `evals/results/specs/dinh-tuyen-sonnet`, `dinh-tuyen-opus` | none — the bar is a GATE-DONE reading, by D-04 | elevated — a live-model count that costs money and cannot be replayed; evidence: baseline recorded in `specs/specs-a-plus/task-10-measure-with-both-doors.md` | runtime: summarizer table from two non-partial result directories |

## Acceptance criteria
| ID | EARS criterion | Proof |
|---|---|---|
| AC-01 | When the skill self-test suite runs after the rewrite, it shall report PASS, with `description:` a non-empty single line in both skills and `argument-hint` unchanged. | `node scripts/run-skill-self-tests.mjs` (from `packages/spec`) prints `[skill-test] PASS` |
| AC-02 | The `cf:specs` and `cf:brainstorm` descriptions shall each state that a request which does not yet say enough to act on belongs at a door rather than at a direct answer, and shall separate the two doors by an observable condition rather than by how abstract the work sounds. | the condition is written down **before** either line is drafted, together with its expected verdict on three prompts already in the repository — `evals/specs/sua-typo` and `evals/specs/khong-kich-hoat` expect no door, `evals/specs/mo-ho-du-cua/case.yaml:12` expects one of the two — and task 01's Receipt applies the condition to each of the three and reports the door it names. A condition that sends the typo prompt to a door fails this criterion |
| AC-03 | When `evals/run.sh specs --with-skill brainstorm --validate` runs after the rewrite, it shall exit 0 with no `✗` line and list the same seven cases as before. | the `--validate` output |
| AC-04 | When `mo-ho-du-cua` runs **twenty times per model** with both doors loaded, the summarizer shall report, per model, how many runs invoked `cf:specs` and how many invoked `cf:brainstorm`, and the Receipt shall state how many invoked neither, derived as `runs` minus `da-goi-mot-skill` because the summarizer prints no such column. The counts are printed beside the 2026-09-21 baseline of 4/10, 0/10 and 6/10 for sonnet and 7/10, 0/10 and 3/10 for opus, and read against the resolution recorded above. | `node evals/summarize.mjs evals/results/specs/dinh-tuyen-sonnet evals/results/specs/dinh-tuyen-opus` |
| AC-06 | When the two negative cases run three times per model with both doors loaded, the skill shall be invoked in none of them, holding the 0/24 that `specs-a-plus` measured and the user accepted at its GATE-DONE. | the four `am-tinh-*` directories report `khong-goi-specs` passing in every run of `khong-kich-hoat` and `sua-typo`; four directories and not two, because `--case` accepts one glob and silently keeps the last when given more |
| AC-05 | Both changelogs shall carry an `## [Unreleased]` `### Changed` entry naming the description change, referencing `skill-routing`, and stating the before and after counts as one sample rather than as properties of the skill. | `grep -l skill-routing packages/spec/CHANGELOG.md docs/project-changelog.md` returns both files |

## Decisions
| ID | Chosen | Rejected | Why | Load-bearing assumption | Break signal → response |
|---|---|---|---|---|---|
| D-01 | Change only the `description` line of two files | Body edits; `when_to_use`/`keywords`/`category`; the other thirty skills; `skill-domain-routing.md` | The user chose KEEP and chose both descriptions over the catalog-field option; the description is the only text measured to move routing, from 0/4 to 10/12 on 2026-09-17 | The host routes on `description`, and the eval harness shows the model the same field | The counts do not move at all → reopen scope for the catalog fields rather than editing further inside this packet |
| D-02 | Two doors, unchanged case, unchanged graders | Six doors; all thirty-two; a new case | Only an unchanged instrument keeps the new counts comparable with the baseline; with a changed instrument a movement cannot be attributed to the description | The harness and the model versions behave as they did on 2026-09-21 (`claudeVersion 2.1.278`) | A version or harness change is observed → re-measure the baseline before reading the new counts |
| D-03 | Success is fewer runs that invoke nothing | Success is the correct door; success is brainstorm ≥ some count | The user's GATE-SCOPE answer, and the opus data where every unrouted run failed and every routed run passed | A run that reaches either door behaves better than one that answers directly, which holds for opus and is untested for sonnet, whose unrouted runs all passed | `cf:specs` rises while `khong-code` or `khong-tu-chot` falls → report both at GATE-DONE and do not re-aim silently |
| D-04 | Tasks record counts; the bar is read at GATE-DONE | A numeric floor inside task 02's Acceptance | Precedent D-10 in `specs-a-plus`: a task whose acceptance is a live-model count cannot be repaired without changing the skill, which is a new packet | The user is willing to judge the numbers once, at the gate | The user asks for a floor instead → set it at GATE-SCOPE of a later round, not inside a task |
| D-05 | The stale `.claude/` and `.agents/` installs stay as they are | Reinstall to sync them | They are gitignored artifacts with no tracked files, and the installed `.claude/` supplies the hooks that gate this packet, so replacing it mid-packet changes the referee during the match | No step of this packet reads the installed copy | A step turns out to need the installed copy → stop and ask the user before reinstalling |
| D-06 | Twenty runs per model, forty new runs, $10-14 | Ten per model at $5-7; ten first and ten more only if the first is unreadable | At ten per model only a drop to 2/20 or below is separable from the baseline and no per-model figure is readable at any value, so the cheaper plan risks paying for a number that cannot be read | The baseline of 9/20 is a fair comparison point and the runs stay independent | The first twenty runs already land at or below 2/40 → the answer is in hand and the remaining runs are a confirmation, not a rescue |
| D-07 | The `cf:brainstorm` line names the other door without a coordination verb | A line that says where to hand off; editing the probe; moving the description out of the corpus | The probe that owns `handoff-boundary` exists to stop Brainstorm from claiming dispatch authority, and that rule is worth more than one phrasing | A description can separate the doors by stating its own entry condition rather than by pointing at the other workflow | Every candidate line fails the probe → stop and return to GATE-SCOPE rather than weakening the probe |

## Tasks
| # | Task | Priority | Criteria | Primary ownership | Dependencies | Status |
|---|---|---|---|---|---|---|
| 01 | Both descriptions cover the under-specified request and divide the doors observably | P1 | AC-01, AC-02, AC-03 | `skills/specs/SKILL.md`, `skills/brainstorm/SKILL.md` | - | done |
| 02 | The two-door case is measured again, and the negative cases are checked for over-trigger | P1 | AC-04, AC-06 | `evals/results/specs/dinh-tuyen-*`, `evals/results/specs/am-tinh-*` | task-01 | done |
| 03 | Both changelogs record the description change with its measured sample | P2 | AC-05 | `packages/spec/CHANGELOG.md`, `docs/project-changelog.md` | task-02 | done |

## Review log
- GATE-REVIEW (2026-09-21): one fresh-context reviewer, 14 findings after deduplication (2 Critical, 3 High, 4 Medium, 5 Low), verdict FAIL. The controller reproduced every Critical and every Medium factual claim before the gate. Two corrections to the report itself: `.agents/` does exist, at `packages/spec/.agents`, not at the repository root; and the reviewer's per-model resolution figure was optimistic — recomputing gives p = 0.211 for opus at 0/10, not 0.028, so no per-model figure is readable at ten runs.
- User decision: **accepted all 14**, chose **twenty runs per model** over ten, and chose to fold the negative-case check into task 02 rather than a separate task.
- Found after the gate by the controller, at $0, while writing the repair for H-02: `--case a --case b` silently keeps only `b` (the validate output read `2 arms × 1 case` and named only `sua-typo`), and `--case '{a,b}'` matches nothing. The negative check therefore runs as one invocation per case per model, six result directories in total. Had this reached execution, the Receipt would have claimed both negative cases while measuring one.
- Applied: C-01 records the brainstorm corpus trap and D-07 constrains the wording; C-02 records the measurement resolution and raises the run count; H-01 replaces `;` with `&&` in task 01's Command; H-02 adds AC-06 and extends task 02; H-03 makes AC-02 falsifiable by pre-registering the condition's verdict on three existing prompts; M-01 corrects three measured figures; M-02 corrects `evals/run.sh:37` to `:38`; M-03 states that "neither" is derived, not printed; M-04 defines the rerun boundary; L-01 through L-05 are recorded as limitations or small corrections.
- Planning is complete. Implementation requires a new explicit `/cf:develop skill-routing` invocation.

## Known limitations carried into execution
- The measurement reads routing with two doors while the product ships thirty-two skills, and the eval plugin does not generate the skill catalogue (`evals/run.sh:56-62`), so `when_to_use` and `keywords` are close to invisible in the measurement but present in the product. Whether a host reads those fields is `[UNVERIFIED]`.
- The shipped `cf:brainstorm` description is **narrower** than its own body, which is the direct consequence of the GATE decision that an under-specified request stays with `cf:specs`. Three surfaces lose their entry from the catalogue while keeping their behaviour in the file: `brainstorm/SKILL.md:5`, where the frozen `when_to_use` still offers "material product, scope, or architecture choices" and now disagrees with the description beside it; `:88`, where the body resolves Outcome for every feature and bug while the description asks for an outcome already agreed; and `:53-62`, the bug/failure and read-only exploration routes, which the description no longer advertises. Nothing in this packet measures any of the three, in either direction.
- The `cf:brainstorm` count in task 02 will almost certainly read 0/20 per model, exactly as the baseline did, and that is not a result. The seven-case suite holds no positive brainstorm case — none where the outcome is agreed and two designs compete — so the money buys the contrast between `cf:specs` and no door at all, and says nothing about whether narrowing brainstorm was right.
- A second pinned-clause trap now sits close to the `cf:brainstorm` description and will bite the next rewrite differently from the first. Its longest overlap with `BRAINSTORM_CONTRACT_CLAUSES.materialChoice` is 42 characters, `"t with meaningfully different consequences"`. If a later line pushes that to a full match, `replaceBrainstormClauseOnce` (`run-skill-self-tests.mjs:1400-1411`) throws `expected one normalized Brainstorm mutation anchor, found 2` and the whole suite stops running rather than failing one test, with a message that points nowhere near the description. The specs side has the same shape at `:1279`. The count is 1 today and the suite passes.
- `.claude/` stays at the installed release, so after task 01 this repository's own session runs a description two generations behind the source; no count measured here describes this session.
