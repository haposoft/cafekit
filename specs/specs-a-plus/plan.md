# Specs A⁺ — ask once, decide visibly, verify by comparison
Specs-Contract: process-first-ready-v1

## Scope decision (GATE-SCOPE — 2026-09-18)
- Existing: four of the eight mechanisms already have a home. Ambiguity has the `decision-needed` action (`packages/spec/src/claude/skills/specs/references/templates.md:160`, pinned); acceptance→task coverage is stated (`templates.md:49-50`) and audited as traceability drift (`packages/spec/src/claude/skills/sync/references/sync-protocols.md:50`); reviewer claim budgets by tier exist (`references/review.md:33-38`); Step 0 already refuses to let wording lower a risk floor (`skills/specs/SKILL.md:16`, pinned). The eval harness (`evals/run.sh`) and four cases (`evals/specs/`) exist; `context.history_file` resumes a `.jsonl` transcript and was proven live on 2026-09-18 (`claude --resume <path.jsonl>` with a two-record transcript kept the `sessionId` and recalled the planted fact; $0.019).
- Minimum change: net zero lines per machine floor. `SKILL.md` must stay within 150–230 lines (`packages/spec/scripts/run-skill-self-tests.mjs:5010-5012`) and is exactly 150, so it absorbs its own additions; `templates.md` has no floor and absorbs the rest. About thirty lines are cut from passages whose headings and layout tokens stay (`templates.md:3-11`, `:139-150`, `:189-197`, `:204-219`; `SKILL.md:90-110`). The literals the `Priority` column changes (`:4263`, anchor rows `:1088-1089`) move with it; three probes with counterexamples join the 5-tuple mutation list (`:864-911`). Two eval cases, one `tags:` line in each existing case, a result summarizer, two small `run.sh` repairs, two changelog entries.
- Expansion signals: seven modified files plus two new case directories exceed the eight-file mark, and the four existing cases gain one line each (GATE-REVIEW F-04); no new service or class; two independently deliverable groups (skill text with its probes; eval cases), below the split threshold.
- User decision: EXPAND — also make the consumers read what the templates now produce: `skills/develop/SKILL.md:60-62`, `rules/workflow.md:22-29`, `agents/spec-maker.md:49-52`. Chosen over KEEP so that `Steps` and `Failure Protocol` act during implementation, not only during planning.

## Scope decision (GATE-SCOPE round 2 — 2026-09-19)
- Trigger: at GATE-DONE the user judged the feature incomplete. Three of the packet's own graders were shown by the measurement to be defective, so two of the five AC-07 bars were measured with a broken instrument and cannot be read as skill results.
- Existing: `evals/specs/mo-ho-c1/graders/khong-tu-chot.md` penalises the minimum-change proposal that `SKILL.md` Flow 1 requires; `evals/specs/sau-keep/graders/moi-ac-co-task.md` rejects range notation (`AC-01..AC-06`) that covers every declared ID, failing 4 of 6 runs across both models; `evals/specs/dung-o-c1/graders/khong-code.md` (copied into `mo-ho-c1`) matches a prose line opening with the word "Export".
- Minimum change: repair those three graders, tag the three affected cases so one invocation per model reruns exactly them, and remeasure. Thirty runs, about $8-14.
- Expansion signals: none. Three grader files, three `tags:` lines, one replay script, two new tasks.
- User decision: KEEP. Excluded on purpose: patching the skill for the opus case that delivered code in chat (that is a skill change and would require a full remeasurement), any new grader such as one for task granularity, and the three cases not affected by these three defects.

## Scope decision (GATE-SCOPE round 3 — 2026-09-20)
- Trigger: the ambiguous case is the only bar still clearly missed, and the runs separate cleanly — in all 12 recorded runs, every run that invoked the skill passed the marker and three-option graders, and every run that did not failed them. The bottleneck is routing, not the skill body.
- Existing: the eval plugin loads exactly one skill (`evals/run.sh:26,33,40`), so a model facing an under-specified request can only use `cf:specs` or answer alone. `cf:brainstorm` — whose own description is "unresolved product or architecture intent", the closest match to this prompt — is not in that world at all, and the four non-invoking runs called no skill whatsoever.
- Minimum change: let the harness load more than one skill, add a case that asks the same ambiguous question with both doors open, and measure which door a model takes. Ten runs per model, single arm, because the question is which skill fires rather than whether the skill helps.
- Expansion signals: none. One `run.sh` option, one new case, two new tasks.
- User decision: KEEP, at ten runs per model rather than three, because the packet's own finding is that three runs cannot separate a real difference from noise. Excluded on purpose: changing the skill description, which is the obvious next lever but would be a guess until this measurement says whether the model has a door it prefers.

## Completion decision (GATE-DONE — 2026-09-21)
The user accepted the feature as complete with all ten tasks `done` and every task carrying a canonical inline Receipt. The AC-07 bar was read against the freshest measurement of each case and is recorded here as met or missed, not rounded up.

| AC-07 bar | Measured | Freshest source | Verdict |
|---|---|---|---|
| Skill invoked in ≥ 10 of 12 positive with-runs | 10/12 (`dung-o-c1` 6/6, `export-csv` 4/6) | tasks 04-05 and task 08 | met, by one run |
| `dung-truoc-khi-lam` passed ≥ 10 of 12 | 9/12 (`dung-o-c1` 6/6, `export-csv` 3/6) | tasks 04-05 and task 08 | missed, by one run |
| Skill invoked 0 of 24 times in the negative cases | 0/24 (`khong-kich-hoat` and `sua-typo`, both arms, both models) | tasks 04-05 | met |
| Every grader ≥ 5 of 6 with-runs of `mo-ho-c1` | 3/6 on four of five graders | task 08 | missed |
| Every grader ≥ 5 of 6 runs of `sau-keep` | 5/6 or 6/6 on all eleven graders | task 08 | met |

Three of the five bars are met. Both misses have the same cause, localised in task 08 and confirmed in task 10: the skill is followed whenever it is invoked and the failures sit almost entirely in runs that invoke nothing. Task 10 opened the second door to test whether the unrouted runs were reaching for a different workflow; with `cf:brainstorm` loaded beside `cf:specs`, zero of twenty runs took it.

The user's decision was to accept completion and to treat routing as a new scope decision for its own packet, because the lever is the skill description, and task 10 forbade touching it precisely so the measurement would stay honest. Known limitations carried out of this packet: routing is unrepaired and its two door counts are 11/20 and 0/20; opus delivers finished code in 3 of 10 unrouted runs; the Skill tool's input encoding is `[UNVERIFIED]` and the guard's false-negative direction is untested; intermediate traces are not retained, so whether an unrouted run considered a skill cannot be read; artifact declarations are inconsistent across the packet, with task 10 the only machine-checkable Receipt and also the only one that can break later because the bytes it pins are gitignored; and task 10's Command and Step 2 were edited after a failed run rather than stopping to ask, which is disclosed in its Receipt.

## Why this is wanted
The user's charter (confirmed 2026-09-17): specs that lead to the right system, feature, or task at any size and project type, close to what the prompt author meant, readable and traceable, and — the bar that matters — a skill the model actually follows, measured by eval. Measured on 2026-09-17 with `claude plugin eval` (sonnet and opus): after the description change the skill is invoked in 10/12 positive runs, but only 7/12 reach GATE-SCOPE correctly. The three named misses: the ask omits one of EXPAND/KEEP/CUT; the planner settles scope items itself; opus treats a request that looks small as `routine`. The plan and task files that a KEEP produces have never been measured.

## Measured constraints (2026-09-18, HEAD `279a115`, worktree clean)
- Bundle budget: **750 lines over nine files, exactly 750/750 today** (`run-skill-self-tests.mjs:722-732`, check at `:947`), legacy `templates/*` included. The 18/08 ceiling of 550 applies to the three process-first files (150 + 127 + 219 = 496). `SKILL.md` alone has a floor of 150 and a ceiling of 230 (`:5010-5012`); `develop/SKILL.md` 140–200 (`:5015-5017`); no other file has a floor.
- Pins that the cut passages touch and must keep: `specs/<feature>/` and `task-NN-<slug>.md` (only occurrences in `templates.md`, `:7` and `:10`; lint `:2579-2583`, probe `:4259-4265`); headings `EARS sentence patterns`, `Twelve edge-case dimensions`, `Quality and saturation checks`, `Example Mapping rule` and the five pattern names (`:2598`, `:4276-4280`, `:5107`); in `SKILL.md` the phrase `` `## Receipt` `` (`:94`, probe `:4207`) and "Placeholder or remembered output is not evidence." (`:105`, probe `:5170`). The Coverage profile header and row are pinned as arrays (`:776-783`); the Tasks example row is a mutation anchor (`:1088-1089`).
- The Codex-installed copy is already checked by the suite (`Codex installed Specs and spec-maker reject adaptive coverage mutations`); there is no separate Codex source mirror to edit.
- Harness facts (`claude plugin eval --help`, CLI 2.1.274): `--case <glob>` takes one glob and silently drops further names; `--tag <tag...>` and `--allow-tools <tools...>` are lists; `--threshold` defaults to 1.0 and exits 1 when any case scores below it; `--model`, `--judge-model`, `--ablation none`, `--runs` exist. `evals/run.sh` passes trailing options through (`:41` validate, `:49` run) and its `--validate` branch exits 2 today because the $0 ceiling trips `set -e` before `exit 0` (`:19`, `:40-43`). `file_exists` accepts a glob and counts only files created during the run; `regex`/`llm` accept `target`/`focus` of `trace | last_message | files | {source: file, path}`; `runs:` is a top-level case field (under `execution:` it is ignored; the four existing cases place it there and only get the default 3 by coincidence).

## Out of scope
- A living-spec layer (OpenSpec `specs/<capability>/spec.md` + change deltas): separate packet.
- Switching the layout or contract to Kiro, spec-kit, or another standard; reopening the 18/08 decision; changing `process-first-ready-v1`; lowering the `SKILL.md` line floor.
- Migrating the eighteen existing packets; editing hooks or resolvers; trimming legacy `skills/specs/templates/*`; changing the Sync skill (its Audit already covers acceptance traceability).
- Changing the behaviour or graders of the four existing eval cases (they gain one `tags:` line each and nothing else).
- Refreshing this repository's installed `.claude/` copy (gitignored, `.gitignore:115`); evals copy the source directly.

## Coverage profile
| ID | Outcome | Change kinds | Material surfaces | Ambiguity/action | Risk/evidence | Required proof |
|---|---|---|---|---|---|---|
| CP-01 | The skill text carries the eight A⁺ mechanisms and every previously pinned sentence, within 750 (nine files), 550 (three files), and the 150–230 floor of `SKILL.md` | modify | `skills/specs/SKILL.md`, `references/templates.md`, `references/review.md`, `scripts/run-skill-self-tests.mjs` | none | elevated — installed behaviour of every future packet; evidence: budget 750/750, floors and pins above | source: self-test PASS with printed totals and mutation count |
| CP-02 | Develop, the workflow rule, and spec-maker read what the templates now produce | modify | `skills/develop/SKILL.md:60-62`, `rules/workflow.md:22-29`, `agents/spec-maker.md:49-52` | none | elevated — cross-component contract; evidence: develop has pinned sentences at `:60` and `:77`, none on `:61-62` | source: consumer probe in the same suite |
| CP-03 | Two eval cases, a tag per case, two `run.sh` repairs, and a result summarizer load; the resumed case runs once inside the harness | add, modify | `evals/specs/mo-ho-c1/`, `evals/specs/sau-keep/`, one `tags:` line in `evals/specs/{dung-o-c1,export-csv,sua-typo,khong-kich-hoat}/case.yaml`, `evals/run.sh:40-43` and its output directory, `evals/summarize.mjs` | none | elevated — external harness contract; evidence: resume probe and help text above | source: `--validate` exit 0 after the repair; live: one `--runs 1` resume with `co-plan` passed |
| CP-04 | Sonnet's runs are recorded completely and summarized from named result directories | other:measure | `evals/results/specs/sonnet-single/`, `evals/results/specs/sonnet-history/` | none | elevated — live adherence is the charter's bar; evidence: baseline 17/09 10/12, 7/12, 0/24 | live: summarizer table, non-partial |
| CP-05 | Opus's runs are recorded the same way and the aggregate over both models is printed once | other:measure | `evals/results/specs/opus-single/`, `evals/results/specs/opus-history/` | none | elevated; evidence: opus called "export CSV" routine in 2/3 runs on 17/09 | live: summarizer table plus aggregate |
| CP-07 | The three defective graders judge what they claim, proven by replaying them against evidence already recorded | fix | `evals/specs/mo-ho-c1/graders/{khong-tu-chot,khong-code}.md`, `evals/specs/sau-keep/graders/moi-ac-co-task.md`, `evals/specs/{export-csv,mo-ho-c1,sau-keep}/case.yaml`, `evals/check-graders.mjs` | none | elevated — every later number depends on these instruments; evidence: 4 of 6 and 1 of 6 measured with them | source: replay over stored evidence plus `--validate` |
| CP-08 | The three affected cases are remeasured on both models with the repaired graders | other:measure | `evals/results/specs/recheck-{sonnet,opus}/` | none | elevated; evidence: the two missed bars | live: summarizer table over the two directories |
| CP-09 | The harness can load several skills into one eval plugin, and a case can tell which of them a run invoked | modify, add | `evals/run.sh`, `evals/specs/mo-ho-du-cua/` | none | elevated — it changes what every later measurement is measuring; evidence: the four runs that invoked nothing | source: `--validate` listing the case with both skills, and the manifest naming both |
| CP-10 | The ambiguous request is measured with both doors open, ten runs per model | other:measure | `evals/results/specs/du-cua-{sonnet,opus}/` | none | elevated; evidence: routing is the bottleneck | live: summarizer table over the two directories |
| CP-06 | The change is recorded for release | modify | `packages/spec/CHANGELOG.md`, `docs/project-changelog.md` | none | routine | source: `test … -eq 2` exit 0 |

## Acceptance criteria
| ID | EARS criterion | Proof |
|---|---|---|
| AC-01 | When the skill self-test suite runs, it shall report PASS with the nine-file bundle at or below 750 lines, the three process-first files at or below 550 lines, `SKILL.md` within 150–230 lines, and every sentence pinned before this packet still present. | `node scripts/run-skill-self-tests.mjs` (from `packages/spec`) prints `[skill-test] PASS` and `total N/750` with N ≤ 750 |
| AC-02 | While the planner follows Flow 1, `SKILL.md` shall require exactly one GATE-SCOPE ask that lists every `decision-needed` item as `[NEEDS CLARIFICATION: <question>]`, offers EXPAND, KEEP, and CUT together, and forbids choosing a product outcome on the user's behalf; and Step 0 shall state that request size is not a risk signal. | new probe `scope-ask-once`; its mutation (drop one of the three words, or the marker) is rejected |
| AC-03 | The plan template shall carry a `## Decisions` table (chosen, rejected, why, load-bearing assumption, break signal → response) and a `Priority` column with the P1–P3 rule; the task template shall carry an ordered `## Steps` list, the sentence that verification compares observed output with the Oracle and never judges, and a `## Failure Protocol` block followed by one worked failure example. | new probe `decisions-steps-failure-protocol`; mutation rejected |
| AC-04 | Before GATE-REVIEW, the skill shall require the author to re-run a sample of the plan's `path:line` citations sized by the claim-budget table in `review.md` and relabel any stale citation `[UNVERIFIED]`. | new probe `fact-check-sample`; mutation rejected |
| AC-05 | When Develop loads a task it shall list `Steps` and `Failure Protocol` among the loaded sections; `rules/workflow.md` shall list both among the extracted fields; `agents/spec-maker.md` shall require a priority, ordered Steps, and a Failure Protocol per task. The Sync Audit already reports an acceptance ID not mapped both ways as traceability drift (`sync-protocols.md:50`) and is not changed. | new consumer probe in the same suite; removing `Failure Protocol` from `develop/SKILL.md:62` fails it |
| AC-06 | When `evals/run.sh specs --validate` runs after the `run.sh` repair, it shall exit 0 only when every case loads and shall list all six cases; when `sau-keep` runs once with `--tag history --threshold 0 --allow-tools Write Edit --out smoke-sau-keep`, its `result.json` shall show one case, `partial: false`, and grader `co-plan` passed. | the task-03 Command output and `evals/results/specs/smoke-sau-keep/result.json` |
| AC-07 | When a measurement task finishes, `evals/summarize.mjs` over its two named result directories shall print, per case and arm, the runs, the `Skill` invocations, and each grader's pass count, reading non-partial files only; when both tasks are done, the aggregate over the four directories shall be printed once. GATE-DONE compares that aggregate with the bar: `Skill` invoked in ≥ 10 of 12 positive with-runs (`dung-o-c1`, `export-csv`), `dung-truoc-khi-lam` passed ≥ 10 of 12, the skill invoked 0 of 24 times in the negative cases (`khong-kich-hoat`, `sua-typo`; both arms), every grader passed in ≥ 5 of 6 with-runs of `mo-ho-c1` and in ≥ 5 of 6 runs of `sau-keep`. | `node evals/summarize.mjs <result directories>` |
| AC-08 | Both changelogs shall carry an `## [Unreleased]` `### Changed` entry naming the `cf:specs` A⁺ mechanisms and the two eval cases, referencing `specs-a-plus`. | `test "$(grep -l specs-a-plus packages/spec/CHANGELOG.md docs/project-changelog.md \| wc -l)" -eq 2` exits 0 |
| AC-09 | When `evals/check-graders.mjs` runs, it shall show that the repaired `khong-code` pattern no longer matches the prose line that failed `export-csv` run 2 while still matching the run that delivered code, and that neither llm rubric still contains the clause that contradicted the skill. | `evals/run.sh specs --validate --allow-tools Write Edit && node evals/check-graders.mjs` |
| AC-10 | When the three tagged cases are rerun on both models with the repaired graders, the summarizer shall print their per-grader counts, and those counts shall be recorded beside the counts measured with the defective graders. | `node evals/summarize.mjs evals/results/specs/recheck-sonnet evals/results/specs/recheck-opus` |
| AC-11 | When `evals/run.sh specs --with-skill brainstorm --validate` runs, the temporary plugin shall declare both skills and the new case shall load with graders that distinguish a `specs` invocation from a `brainstorm` one. | the validate output and the generated manifest |
| AC-12 | When the ambiguous case runs ten times per model with both skills available, the summarizer shall report, per model, how many runs invoked `cf:specs`, how many invoked `cf:brainstorm`, and how many invoked neither. | `node evals/summarize.mjs evals/results/specs/du-cua-sonnet evals/results/specs/du-cua-opus` |

## Decisions
| ID | Chosen | Rejected | Why | Load-bearing assumption | Break signal → response |
|---|---|---|---|---|---|
| D-01 | Net zero lines per file floor: `SKILL.md` cuts exactly what it adds; `templates.md` absorbs the remaining cuts; legacy `templates/*` untouched | Cut −8 from `SKILL.md`; trim legacy files; raise the 750 ceiling; lower the 150 floor | `SKILL.md` is at its 150 floor (`:5010-5012`); legacy files are frozen by the non-goal; both ceilings are user decisions | The unpinned passages in task 01's ledger free at least as many `templates.md` lines as the mechanisms add there | `bundle line budget` or the `stays lean` probe fails → cut further from the EARS list or the dimensions block, never a pinned sentence or heading |
| D-02 | `Priority` is one column in the Tasks table; tasks are numbered in priority order and the table follows filename order | Column in the Coverage profile or Acceptance table; a table order that differs from filenames | Those headers are pinned as arrays (`:776-783`); the resolver lists tasks by sorted filename (`spec-resolver.cjs:241-244`) and Develop selects "the first dependency-valid `pending` row in `plan.md` order" (`develop/SKILL.md:77`), so numbering by priority keeps both answers identical | No script parses the Tasks table (grep of `scripts/*.cjs`, `hooks/*.cjs`, `bin/*.js` on 2026-09-18: none; the self-test reads Status as the last cell, `:1005-1009`) | A parser appears → keep `Priority` as the last column |
| D-03 | `## Failure Protocol` is a short block using Develop's own words — repair only the cited cause; after three failed rounds, stop and ask the user (`develop/SKILL.md:137-139`) — plus one worked example | ak-plan's verbatim STOP protocol; a block that also sets `Status: blocked` | Law A3: one fact, one home; Develop owns the repair loop and does not itself write `blocked`, Sync does (`sync/SKILL.md:39-40`) | Develop's three-round rule stays as it is | The rule changes → update the block once in `templates.md` |
| D-04 | `mo-ho-c1` is single-turn; `sau-keep` resumes a history | Both multi-turn | The first reply is what GATE-SCOPE measures; a single-turn case keeps ablation, so routing is measured for free | With ablation `auto` or `none`, the harness runs a history case in the `with` arm only (`Yi` returns `["with"]`) | Ablation runs on `sau-keep` → keep `--ablation none` in its invocation |
| D-05 | Content graders for `sau-keep` use `target: trace` (Write tool inputs) and `file_exists` with globs for paths | `regex` with `{ source: file, path }` | Trace sees every message; `file_exists` glob support is verified in the CLI schema | Write inputs appear JSON-encoded in the trace | A grader never matches on a run that visibly wrote the section → switch that grader to `{ source: file, path }` |
| D-06 | The resumed prompt names the skill ("dùng skill specs") | Rely on the model re-routing on its own | The history holds text only, so the skill body is not in context on resume; `sau-keep` measures adherence, the other cases measure routing | The plugin's `Skill` tool is available on resume | `co-goi-skill` fails on every run → include the Skill tool_use record in the history |
| D-07 | `--judge-model sonnet` for both measurement tasks | Harness default judge | Docs: a small judge marks correct answers wrong when the format differs; guidance says judge ≥ sonnet-tier | Thresholds are absolute, so the 17/09 baseline (default judge) is a reference, not a control | A `dung-truoc-khi-lam` vote contradicts the visible reply → read the judge's excerpt in `report.html` before changing the skill |
| D-08 | This packet already uses the new sections | Plain current template | It shows the shape task 01 must produce; the resolver parses only the `Specs-Contract` marker, `Status:`, and `## Dependencies` (`spec-resolver.cjs:99-103, 109, 123-176`) and the gate parses `## Verification Plan` and `## Receipt` (`spec-receipt.cjs:140,171`), so extra sections are inert | — | A gate error names an unknown section → remove it here and report |
| D-09 | Cases carry `tags:` (`single-turn` on five, `history` on `sau-keep`) and each measurement uses two `--tag` invocations | One invocation per model with `Write Edit` granted to every case; `--case` lists | `--case` takes one glob; granting Write to the routing cases would change their environment against the 17/09 baseline and `khong-code` reads only the last message | `--tag` filters by any listed tag (verified in help: `<tag...>`) | A routing case starts writing files anyway → add a `tool_used Write max 0` grader to it in a later packet |
| D-10 | `--threshold 0` on every measurement invocation; `evals/summarize.mjs` is the only counter; tasks 04–05 only record, GATE-DONE judges | Default threshold; hand-typed counts; a task that fails when a floor is missed | The default threshold exits 1 when any run misses a grader, which the bar itself allows; a task whose acceptance is a live-model count cannot be repaired without changing the skill, which is a new packet, so the count is a GATE-DONE input | The harness exit code is not part of any Receipt oracle | The summarizer disagrees with `report.html` → the report is authoritative and the summarizer gets a test |
| D-11 | `run.sh --out <name>` fixes the result directory and refuses to overwrite; the summarizer takes explicit directories and skips `partial: true` files | Timestamped directories plus a `--since` filter | A date filter would pool the smoke run, aborted runs, and retries into one denominator; explicit names let the Command be written before it runs and match the Receipt byte for byte | Aborted runs are removed by the operator before a rerun | A stale directory blocks a rerun → the Failure Protocol names the explicit removal |

## Tasks
| # | Task | Priority | Criteria | Primary ownership | Dependencies | Status |
|---|---|---|---|---|---|---|
| 01 | Skill text carries the eight mechanisms, net zero per floor, probes updated | P1 | AC-01, AC-02, AC-03, AC-04 | `skills/specs/*`, `scripts/run-skill-self-tests.mjs` | - | done |
| 02 | Consumers read the new sections | P1 | AC-05 | `skills/develop/SKILL.md`, `rules/workflow.md`, `agents/spec-maker.md` | task-01 | done |
| 03 | Two eval cases, tags, two `run.sh` repairs, and a summarizer load; the resumed case runs once | P1 | AC-06 | `evals/specs/*`, `evals/run.sh`, `evals/summarize.mjs` | - | done |
| 04 | Sonnet recorded and summarized | P1 | AC-07 | `evals/results/specs/sonnet-*` | task-01, task-02, task-03 | done |
| 05 | Opus recorded, summarized, and the aggregate printed | P2 | AC-07 | `evals/results/specs/opus-*` | task-01, task-02, task-03, task-04 | done |
| 06 | Changelogs record the change | P3 | AC-08 | `packages/spec/CHANGELOG.md`, `docs/project-changelog.md` | task-01, task-02, task-03 | done |
| 07 | The three defective graders are repaired and the affected cases tagged | P1 | AC-09 | `evals/specs/*/graders/*`, `evals/specs/*/case.yaml`, `evals/check-graders.mjs` | task-06 | done |
| 08 | The three affected cases are remeasured on both models | P1 | AC-10 | `evals/results/specs/recheck-*` | task-07 | done |
| 09 | The harness loads several skills and a case records which one fired | P1 | AC-11 | `evals/run.sh`, `evals/specs/mo-ho-du-cua/` | task-08 | done |
| 10 | The ambiguous request is measured with both doors open | P1 | AC-12 | `evals/results/specs/du-cua-*` | task-09 | done |

## Review log
- Round 1 (2026-09-18): two fresh-context reviewers (Fact Checker + Assumption destroyer + Contract Verifier on plan and tasks 01–02; Flow Tracer + Scope critic + Proof lens on tasks 03–06). 16 findings, one duplicate merged → 15 (5 Critical, 5 High, 5 Medium). Every finding was reproduced by the controller before GATE-REVIEW. User: accepted all 15 with the proposed repairs; chose tags with two invocations per model (F-04) and measure-only tasks with the bar judged at GATE-DONE (F-09). Sweep and closure table below.
- Sweep 2026-09-18 after the repairs: `files reread 7 / deltas 15 / stale references fixed 11 patterns (remaining mentions of --case and --since are harness facts or rejected alternatives, none in a Command) / conflicts left 0`. Resolver re-read: `process-v3`, six tasks, dependencies 0/1/0/3/4/3.

### Accepted-repair closure (Round 1)

| ID | Decision | Original counterexample | Repaired at | Proved at | Replay | Closure |
|---|---|---|---|---|---|---|
| F-01 | accepted | cutting −8 from `SKILL.md` lands at 148, below the 150 floor (`:5010-5012`) | task-01 Steps 2–3; D-01 | sweep: per-file ledger present (`+5 (net 0, stays at 150)`, targets −5 / −23) | arithmetic per file: 150 − 5 + 5 = 150; 219 − 23 + 16 = 212; 127 + 3 = 130; bundle 746 ≤ 750 | PASS |
| F-02 | accepted | one-line intro loses `specs/<feature>/` and `task-NN-<slug>.md`, lint `templates-flat-layout-drift` | task-01 Step 2, `:3-11` bullet | sweep: the bullet names all three tokens (1 match) | grep of the bullet for the three tokens | PASS |
| F-03 | accepted | `evals/run.sh specs --validate; echo $?` → 2, chain dies | task-03 Step 1; AC-06 | sweep: AC-06 says "exit 0 only when every case loads"; task-03 Step 1 repairs the branch; unmodified `run.sh` still exits 2 today (measured) | paper: the packet no longer treats the unmodified exit code as a pass; runtime replay is task-03's Command | PASS |
| F-04 | accepted | `--case a b` runs one case | tags in task-03 Steps 3–4, 7; `--tag` in task-04/05 Commands; D-09 | sweep: `--case` appears 0 times in any Command; `--tag` 7 mentions | grep of every `- Command:` line | PASS |
| F-05 | accepted | default `--threshold 1.0` exits 1 when a run misses a grader | `--threshold 0` in the three measurement Commands; D-10 | sweep: 3 of 3 measurement Commands carry `--threshold 0` | grep of `- Command:` lines | PASS |
| F-06 | accepted | Tasks example row is a mutation anchor (`:1088-1089`) | task-01 Scope, Step 4 | sweep: `:1088-1089` named 5 times across plan and task-01 | grep | PASS |
| F-07 | accepted | Flow 4–5 condensation drops `` `## Receipt` `` or the Placeholder sentence | task-01 Step 2, Flow 4–5 bullet | sweep: both phrases named as keep-verbatim (1 each) | grep | PASS |
| F-08 | accepted | cut ranges start at pinned headings; inventory 15/56/13 not reproducible | task-01 Step 2 keeps four headings and five names; Acceptance demotes the inventory | sweep: 3 keep-heading bullets; "authoring aid, not an acceptance number" present; `same 15` 0 hits | grep | PASS |
| F-09 | accepted | task 04/05 Oracle demands a floor the task may not repair | task-04/05 Outcome, Acceptance, Oracle; AC-07; D-10 | sweep: both Oracles read "table lists …", `≥ 4/6` 0 hits | grep | PASS |
| F-10 | accepted | `--since` pools smoke, aborted, and retried runs | `--out` named directories; summarizer takes paths and skips partial; D-11 | sweep: `--out` 5 times in Commands; explicit directories in both summarizer calls; `--since` only as a rejected alternative | grep | PASS |
| F-11 | accepted | stale citations (`:481-498`, `:949`, `run.sh:56`, `:1-19`), D-04 missing the ablation condition, D-08 incomplete, `runs` placement | plan § Measured constraints, D-04, D-08; task-01 Scope; task-03 Ownership and Steps 4, 7 | sweep: `:947` 2, `:864-911` 4, `:41`/`:49` 1, `:1-18` 2, D-04 condition 1, D-08 parse list 1, top-level `runs` 1; old spellings 0 | grep | PASS |
| F-12 | accepted | priority order versus filename order gives two "next task" answers | task-01 Step 3 rule; D-02 | sweep: rule present in task-01 and D-02 | grep | PASS |
| F-13 | accepted | Failure Protocol writes `blocked` with no home; Sync item duplicates `sync-protocols.md:50` | D-03; every task's block uses Develop's wording; task-02 drops Sync; AC-05 cites the existing rule | sweep: Develop wording in 7 files; `set \`Status: blocked\`` 0 hits; task-02 Ownership has no sync entry; AC-05 cites `sync-protocols.md:50` | grep | PASS |
| F-14 | accepted | no task owns the two-model aggregate | task-05 Command and Outcome; AC-07 | sweep: task-05 summarizer call lists 4 directories | grep | PASS |
| F-15 | accepted | `grep -n` exits 0 with one of two files matching | task-06 Command; AC-08 | replay in scratchpad: one matching file → exit 1; both → exit 0 | executed `test … -eq 2` | PASS |
