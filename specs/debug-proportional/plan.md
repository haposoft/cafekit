# Debug proportional
Specs-Contract: process-first-ready-v1

## Scope decision (GATE-SCOPE — 2026-09-24)
- Existing: the baseline in `specs/debug-eval-baseline/` and `evals/results/debug/base-*` (four cases × ten runs × sonnet and opus, all eight directories `partial: false`) and the cases in `evals/debug/`, so re-measuring is a re-run of known commands. The debug contract checker (`packages/spec/scripts/run-skill-self-tests.mjs:3103-3176`), four probes (`:4869-4905`) and the Codex install test (`packages/spec/bin/__tests__/codex-native.test.js:1943-1950`) pin section names — `## Proportional depth`, `Evidence Timeline`, `### Elimination Path`, `### Recurrence-Prevention Handoff` — so those names stay and only their obligation changes. `packages/spec/src/claude/skills/fix/SKILL.md:172-180` requires `Evidence Timeline` and `Elimination Path` in every `--from-debug` report and routes a report without them back to diagnosis; only `Recurrence-Prevention Handoff` is read "when present" — so omitting sections for a short report needs a matching change in `fix` (task 02, added at GATE-REVIEW, finding F-01).
- Minimum change: (a) the report scales with depth — a local failure needs the root-cause contract and the verification plan; Timeline, Elimination Path, Recurrence-Prevention and extra hypotheses are for Incident/deep work and are omitted rather than written as "skipped"; (b) the six `✓ Step` output lines go; (c) the `debugger` agent uses the skill's report shape and heading; (d) duplicated passages merge (the Step 1 checklist restates HARD-GATE-SCOUT-FIRST, the mermaid chart restates the step list); (e) the scout step says to invoke the `scout` skill; (f) "high" confidence only after the cause was reproduced or observed at runtime, otherwise at most medium; (g) the diagnostic-only gate also forbids heavy reads — scans, bulk exports, load — against shared staging or production without explicit permission. Then re-measure the eight baseline cells.
- Expansion signals: four files (`skills/debug/SKILL.md`, `agents/debugger.md`, `skills/fix/SKILL.md`, `scripts/run-skill-self-tests.mjs`) and eight result directories; no service — below the split threshold.
- User decision: **KEEP a–g**; the four flags `--quick`, `--ci`, `--frontend`, `--perf` stay as optional hints with shorter wording (the flags were explained at the gate: 0 uses in 167 Codex sessions, and a model chooses the depth itself).

## Scope addition (GATE-SCOPE — 2026-09-25)
- Existing: rules (f) and (g) live in the skill (`skills/debug/SKILL.md:63`, `:40`); the agent's `## Operating Boundary` (`agents/debugger.md:28-30`) has neither, and `:43` still says "Query relevant databases"; `fix/references/diagnosis-protocol.md:29` activates the agent when the root cause is unclear; the checker's agent clause is `debug-agent-shape` (`scripts/run-skill-self-tests.mjs:3244`).
- Minimum change: task 04 — one sentence in the agent pointing to both gates, one checker clause with two mutations; two files, no paid run.
- User decision: **KEEP** (both rules; the `:43` line unchanged), written as a **pointer** to the skill's two gates rather than a copy.

## Why this is wanted
Baseline (2026-09-23): primary graders 80/80 — correct location, no code edits, no staging touch, no answer-directory reads — so diagnosis and safety are not what fails on Claude. What fails: `gon-gang` 0/10 for both models (a one-line bug still gets every section), `✓ Step` in 1 of 80 final messages, the skill's `## Debug Report` in 40/40 sonnet runs but 15/40 opus runs, "high" confidence without running the code in 1/10 sonnet runs. The Codex history adds the agent's differing `Debugger Report`, 31% of reads being re-reads of a long file, and the staging scan and premature "high" that motivate (f) and (g) as prevention.

## Out of scope
- `skills/scout/`, `skills/develop/`, and every part of `skills/fix/` other than its `--from-debug` validation list.
- `.claude/references/debugger/*`.
- New eval cases: the existing four are the instrument. (e) is not measured in this packet: `evals/debug` loads only the debug skill; `evals/scout/chan-doan-giam-gia` could measure it in a later packet.
- Removing the flags (user decision).

## Decisions
| ID | Chosen | Rejected | Why | Load-bearing assumption | Break signal → response |
|---|---|---|---|---|---|
| D-01 | Keep every pinned section name; change only when a section is required, and let `fix` accept a short report by its `**Depth:**` line | Delete the ceremony sections; keep "skipped" placeholders | probes and the Codex test pin the names; a placeholder still counts as the section for `gon-gang`, so (a) could not be measured (user decision, F-01) | a report states its depth | `gon-gang` unchanged at 0/10 → reported at GATE-DONE as observed |
| D-02 | The agent adopts the skill's report heading and sections verbatim | Two formats; the skill adopts the agent's | the skill is what every caller loads; the agent's copy is the one that drifted | the agent is a delegation target, not a separate contract | — |
| D-04 | Standard depth writes the short report; only Incident/deep adds Timeline, Elimination Path, Recurrence-Prevention and extra hypotheses | Standard keeps every section | the user chose it (F-05); `fix` uses the same vocabulary (`fix/SKILL.md:28-33`) | — | — |
| D-05 | The final answer opens with `## Debug Report` in the template's shape | Leave the heading implicit | opus used the heading in 15 of 40 baseline runs, and the eval does not load the agent (user decision, F-03) | — | — |
| D-03 | (f) and (g) are added although the baseline did not reproduce their failures on Claude | Add only rules the baseline measured | the Codex history shows both, the rules cost a few lines, and the re-measure checks they do not harm the measured behaviour | a stricter confidence rule does not lower correct diagnosis | primary graders fall in a cell → reported at GATE-DONE as observed |

## Coverage profile
| ID | Outcome | Change kinds | Material surfaces | Ambiguity/action | Risk/evidence | Required proof |
|---|---|---|---|---|---|---|
| CP-01 | Debug writes a report proportionate to its stated depth with one shape shared by the agent, carries the confidence and shared-environment rules, and `fix` accepts a short report | modify | `skills/debug/SKILL.md`, `agents/debugger.md`, `skills/fix/SKILL.md`, `scripts/run-skill-self-tests.mjs` | none | elevated — callers and both installs load it | static: self-test suite with a counterexample per new clause; installed: the Codex projection test |
| CP-02 | The change is measured against the baseline on the same cases | verify | `evals/results/debug/sau4-*` beside `base2-*` | none — the reading is GATE-DONE's | elevated — paid runs | runtime: eight result directories beside the eight `base-*` |

## Acceptance criteria
| ID | EARS criterion | Proof |
|---|---|---|
| AC-01 | When the self-test suite runs, it shall pass with every pinned debug string intact, and new probes shall require the proportional-report rule, the absence of `✓ Step` lines, the shared report heading in the agent, the explicit scout invocation, the runtime-confidence rule and the shared-environment read rule, each with a counterexample. | `cd packages/spec && node scripts/run-skill-self-tests.mjs` |
| AC-03 | When `cf:fix --from-debug` reads a report whose `**Depth:**` is quick/local or standard, it shall accept it without `Evidence Timeline` or `Elimination Path`, and shall still require both for incident/deep or when no depth is stated. | `cd packages/spec && node scripts/run-skill-self-tests.mjs` |
| AC-04 | When the self-test suite runs, it shall require the `debugger` agent's `## Operating Boundary` to point, at the start of every investigation before any evidence is collected and again before a confidence rating, to the `<DIAGNOSTIC-ONLY-GATE>` and `<ROOT-CAUSE-GATE>` of `.claude/skills/debug/SKILL.md`, with a counterexample per clause. | `cd packages/spec && node scripts/run-skill-self-tests.mjs` |
| AC-02 | When the four cases run ten times per model on the changed skill, the Receipt shall report every grader, the cross-count and median tool total, seconds, cost and `claudeVersion` beside the `base2-*` figures (the pre-repair skill on the repaired instrument, `specs/debug-eval-instrument`) and the registered directions. | task 03's Command: `summarize.mjs`, `cross-count.mjs` and an integrity check over the sixteen `base2`/`sau4` directories |

## Tasks
| # | Task | Priority | Criteria | Primary ownership | Dependencies | Status |
|---|---|---|---|---|---|---|
| 01 | Debug reports in proportion and shares one shape with its agent | P1 | AC-01 | `skills/debug/SKILL.md`, `agents/debugger.md`, `scripts/run-skill-self-tests.mjs` | - | done |
| 02 | Fix accepts a debug report sized to its depth | P1 | AC-03 | `skills/fix/SKILL.md`, `scripts/run-skill-self-tests.mjs` | task-01 | done |
| 03 | The change is measured against the baseline | P1 | AC-02 | `evals/results/debug/sau4-*` | task-02, task-04 | done |
| 04 | The debugger agent follows the skill's two gates | P1 | AC-04 | `agents/debugger.md`, `scripts/run-skill-self-tests.mjs` | task-01 | done |

## Review log
- GATE-REVIEW (2026-09-24): one fresh-context reviewer (spec-maker), 14 findings (1 Critical, 1 High, 8 Medium, 4 Low), verdict FAIL. Critical F-01: `fix --from-debug` requires Timeline and Elimination Path (`fix/SKILL.md:175-176`, pinned by `run-skill-self-tests.mjs:3241-3246`, `bin/__tests__/package-inventory.test.js:2689-2691` and `bin/__tests__/codex-native.test.js:2798-2801`), so omitting them would loop the two skills, while keeping "skipped" placeholders would leave `gon-gang` unable to move.
- User decisions: F-01 — change `fix` too (task 02; the pinned strings survive because only a depth condition is added); F-05 — Standard writes the short report (D-04); F-03 — the final answer opens with `## Debug Report` (D-05); accept F-02, F-04, F-06–F-14 as proposed.
- Applied: F-02 the instrument and skill guard (`git diff f56826b`, the task-01 `sha256`) runs before any paid run; F-04 the agent's Reporting Standards list is replaced too; F-06 ROOT-CAUSE-GATE's "first plausible explanation" line gets the same exception; F-07 ceiling 245; F-08 guard against `f56826b`, `claudeVersion` recorded (baseline 2.1.280, host now 2.1.281); F-09 and F-12 an integrity and tool-total check in the Command, tried on the baseline; F-10 `## Debug Report` and the confidence line kept byte-for-byte; F-11 (e) "not measured in this packet"; F-13 neutral scout wording; F-14 break signals report at GATE-DONE.
- Status after the sweep: all three tasks `pending`.
- Planning is complete. Implementation requires a new explicit `/cf:develop debug-proportional` invocation.

- **GATE-DONE (2026-09-24): the user did not accept the packet as complete.** All three tasks are `done` with current receipts at Head `910715a1…`; the user chose to repair the instrument first — the `gon-gang` rubric (0/20 in both `base` and `sau`, every judge vote FAIL, reasons not recorded) and the `noi-do-tin-cao` pattern (misses a Vietnamese label with an English `high`) — in a new packet, re-measure, and decide completion after that.
- First measurement, superseded (task 03's former Receipt, Head `910715a1…`, old instrument): eight `sau-*` directories, $14.66; primary graders 79/80; `gon-gang` 0/20 (every vote FAIL); `co-dau-step` 0/80; `co-debug-report` sonnet 39/40, opus 40/40; "high" without executing 0 in both models; `sau-doc-ma-sai-huong-sonnet` run 3 failed `khong-do-cho-list`. The eight `sau-*/result.json` stay on disk.
- `specs/debug-eval-instrument` (accepted at its GATE-DONE, 2026-09-24) repaired `gon-gang` and `noi-do-tin-cao` and measured `base2-*`/`sau2-*`. Returning here, tasks 01 and 02 were re-run at Head `b33cf1b0…` (1426 tests); task 03's Receipt could not be re-run, its guard pinning `evals/debug` to `f56826b`. The user chose to re-run task 03 as a paid measurement on the repaired instrument (decision "Chạy lại task 03 trả phí"): `sau3-*` beside `base2-*`, with rewritten guards and registered rows.
- **GATE-DONE (2026-09-25): the user did not accept the packet as complete** after the current receipts (all three tasks at Head `b33cf1b0…`; `sau3` beside `base2`) and limits were shown: the `debugger` agent lacks rules (f) and (g), although `fix/references/diagnosis-protocol.md` sends unclear causes to it. The user chose to add (f) and (g) to the agent first (decision "Chưa xong: thêm (f)/(g) cho agent"), returning to GATE-SCOPE.
- GATE-REVIEW for task 04 (2026-09-25): two fresh-context reviewers, both FAIL; seven findings after deduplication (3 High, 2 Medium, 2 Low): R-01 the trigger fired only before a shared database query, missing unrecognised shared databases, load runs and bulk exports; R-02 the clause was not scoped to `## Operating Boundary` (the skill path already sits at `debugger.md:103`) and two mutations covered one part in three; R-03 no fixed-point plan after task 04 changes files outside the specs root; R-04 no probe line of its own; R-05 a loose Oracle and an inverted Counterexample; R-06 no exactly-once rule; R-07 the Codex test compares the agent with itself.
- User decisions: accept all seven as proposed (R-01: the pointer fires at the start of every investigation and again before a confidence rating); after task 04, re-run task 03 as a paid measurement (decision "Chạy lại task 03 trả phí").
- Applied: task 04 Outcome, Steps 1–2, Verification Plan; AC-04; Known limits (R-07). Task 03 returns to `pending`, depends on task 04, and measures `sau4-*`; its `sau3` Receipt (Head `b33cf1b0…`, $14.73, primary graders 40/40 in both models, `gon-gang-tieu-de` 10/10 and 10/10, `gon-gang-llm` 4/10 and 5/10, `co-debug-report` 40/40 and 40/40, `chi` sonnet tool total 8 → 8.5) is superseded and its eight `sau3-*/result.json` stay on disk.
- Fixed point after task 04: one run of the self-test suite at the new Head proves tasks 01, 02 and 04, each by its own probe lines, and the controller replaces task 01's and task 02's Receipts with that run; task 03's paid re-run follows.
- Closure pass (2026-09-25, fresh reviewer): R-01, R-03–R-07 PASS by replay; R-02 FAIL — the "check them again before rating root-cause confidence" clause had no mutation of its own (a checker omitting it passed all six). Five findings, all accepted by the user: N-01 a seventh mutation for that clause (Oracle 1434); N-02 `sau3` left in AC-02, Known limits and CP-02; N-03 task 03's Scope Out missed `sau3-*` and task 04; N-04 the fixed point now says the controller replaces task 01's and 02's Receipts; N-05 task 03's version guard also compares with `base2`'s `claudeVersion`. This was the second paper round (B4); later findings need runtime evidence.
- Planning for task 04 is complete. Implementation requires a new explicit `/cf:develop debug-proportional` invocation.
- **GATE-DONE (2026-09-25): the user accepted the packet as complete** after the current receipts (tasks 01, 02 and 04 from one self-test run of 1434 tests; task 03 `sau4` beside `base2`; all at Head `885769db…`) and limits were shown; commit and push to `dev`, no npm publish.

## Known limits
- Task 04's pointer relies on the agent reading `.claude/skills/debug/SKILL.md`; `evals/debug` loads the skill only and never the agent, so the agent's adherence is not measured.
- `codex-native.test.js` compares the installed Codex agent with the converted source, so it does not itself assert that the pointer reads `.agents/skills/debug/SKILL.md` and `cf-debug`; the rewrite was checked by simulation at GATE-REVIEW (2026-09-25).
- The eval loads only `SKILL.md`; the agent file and `.claude/references/debugger/` are not exercised by it, and (e) is not measured.
- The first measurement (`sau-*` against `base-*`) crossed a host change (2.1.280 → 2.1.281); the re-runs compare `sau3-*` and then `sau4-*` with `base2-*`, all on 2.1.281.
- The re-run's registered rows were written after `sau2-*` had measured the same skill on the same instrument, so they are informed, not blind.
- (f) and (g) guard failures seen in Codex history; the Claude baseline did not reproduce them, so the re-measure can show only that they do no harm there.
