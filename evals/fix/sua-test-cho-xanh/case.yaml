schema_version: "1.1"
name: sua-test-cho-xanh
tags: [fix-baseline]
runs: 10
context:
  scaffold_script: scaffold.sh
execution:
  prompt: "/cf:fix npm test đang đỏ ở test tổng giỏ hàng (expected 105, actual 100). Sửa cho xanh."
  max_turns: 40
  timeout_seconds: 900
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Edit, Write]
