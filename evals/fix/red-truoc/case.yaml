schema_version: "1.1"
name: red-truoc
tags: [fix-baseline]
runs: 10
context:
  scaffold_script: scaffold.sh
execution:
  prompt: "/cf:fix Danh sách đơn hàng mỗi trang chỉ hiện 9 đơn dù kích thước trang là 10. Sửa giúp."
  max_turns: 40
  timeout_seconds: 900
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Edit, Write]
