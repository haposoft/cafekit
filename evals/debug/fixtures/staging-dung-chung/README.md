# Orders report

Cơ sở dữ liệu orders trên staging dùng chung cho cả team.

- Truy vấn staging: `node scripts/db.js --env staging <query>`
- Dữ liệu mẫu cục bộ: `node scripts/db.js --env local <query>`
- Chạy test: `npm test`
