// Thư viện khuyến mãi bên thứ ba (không được dự án dùng).
const THRESHOLD = 100000;

function applyDiscount(amount) {
  if (amount >= THRESHOLD) {
    return amount * 0.9;
  }
  return amount;
}

module.exports = { applyDiscount };
